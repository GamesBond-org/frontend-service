name: Microservice CI Pipeline

on:
  push:
    branches:
      - develop

  pull_request:
    branches:
      - develop

jobs:
  ci:
    uses: GamesBond-org/database/.github/workflows/reusable-workflow.yaml@develop
    with:
      image_name: frontend-service
      sonar_project_key: gamesbond-frontend-service
    secrets: inherit